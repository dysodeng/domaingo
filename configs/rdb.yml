rdb:
  main:
    enabled: true
    driver: postgresql
    host: localhost
    port: 5432
    username: postgres
    password: password
    database: mydb
  secondary:
    enabled: true
    driver: postgresql
    host: localhost
    port: 5432
    username: postgres
    password: password
    database: mydb
